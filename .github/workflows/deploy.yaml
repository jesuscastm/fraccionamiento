name: Deploy

on: 
  push:
    branches: ["main"]

jobs:
  fist-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Variable
        run: echo "${{secrets.TEST_TOKEN}}"
      - name: setup
        uses: actions/setup-node@v4
        with:
          node-version: '18'